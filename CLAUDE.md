# CLAUDE & OPENCODE INVIOLABLE RULES

## Absolute Directives
1. **ALWAYS WRITE IN LIN (`.lin`) OR RULEL (`.rulel`)**:
   - Every piece of new code, analysis, discovery, testing, and logic MUST be written in `.lin` or `.rulel`.
2. **ZERO NEW ZIG FILES**:
   - NEVER create new `.zig` files.
   - Zig is strictly the Stage 0 minimal bootstrap runtime.
