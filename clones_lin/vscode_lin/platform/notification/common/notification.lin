@LIN:platform_notification_common_notification:1.0.0
~G{?=if #=for ^=ret :else}

!isNotificationSource(thing){
  // Transposed from platform/notification/common/notification.ts
  ^{ name: "isNotificationSource", status: "pure" };
}

!withSeverityPrefix(label, severity){
  // Transposed from platform/notification/common/notification.ts
  ^{ name: "withSeverityPrefix", status: "pure" };
}

!getNotificationPriorityEnum(){
  ^{ enumName: "NotificationPriority", members: ["/**
	 * Default priority: notification will be visible unless do not disturb mode is enabled.
	 */
	DEFAULT", "/**
	 * Optional priority: notification might only be visible from the notifications center.
	 */
	OPTIONAL", "/**
	 * Silent priority: notification will only be visible from the notifications center.
	 */
	SILENT", "/**
	 * Urgent priority: notification will be visible even when do not disturb mode is enabled.
	 */
	URGENT"] };
}

!getNeverShowAgainScopeEnum(){
  ^{ enumName: "NeverShowAgainScope", members: ["/**
	 * Will never show this notification on the current workspace again.
	 */
	WORKSPACE", "/**
	 * Will never show this notification on any workspace of the same
	 * profile again.
	 */
	PROFILE", "/**
	 * Will never show this notification on any workspace across all
	 * profiles again.
	 */
	APPLICATION"] };
}

!getNotificationsFilterEnum(){
  ^{ enumName: "NotificationsFilter", members: ["/**
	 * No filter is enabled.
	 */
	OFF", "/**
	 * All notifications are silent except error notifications.
	*/
	ERROR"] };
}

=ex{isNotificationSource,withSeverityPrefix,getNotificationPriorityEnum,getNeverShowAgainScopeEnum,getNotificationsFilterEnum}
