@LIN:base_node_pfs:1.0.0
~G{?=if #=for ^=ret :else}

!whenDeleted(path, intervalMs = 1000){
  // Transposed from base/node/pfs.ts
  ^{ name: "whenDeleted", status: "pure" };
}

!stat(path){
  // Transposed from base/node/pfs.ts
  ^{ name: "stat", status: "pure" };
}

!configureFlushOnWrite(enabled){
  // Transposed from base/node/pfs.ts
  ^{ name: "configureFlushOnWrite", status: "pure" };
}

!writeFileSync(path, data, options?){
  // Transposed from base/node/pfs.ts
  ^{ name: "writeFileSync", status: "pure" };
}

!realcase(path, token?){
  // Transposed from base/node/pfs.ts
  ^{ name: "realcase", status: "pure" };
}

!realpathSync(path){
  // Transposed from base/node/pfs.ts
  ^{ name: "realpathSync", status: "pure" };
}

!getRimRafModeEnum(){
  ^{ enumName: "RimRafMode", members: ["/**
	 * Slow version that unlinks each file and folder.
	 */
	UNLINK", "/**
	 * Fast version that first moves the file/folder
	 * into a temp directory and then deletes that
	 * without waiting for it.
	 */
	MOVE"] };
}

=ex{whenDeleted,stat,configureFlushOnWrite,writeFileSync,realcase,realpathSync,getRimRafModeEnum}
