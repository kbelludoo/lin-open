@LIN:base_parts_storage_common_storage:1.0.0
~G{?=if #=for ^=ret :else}

!isStorageItemsChangeEvent(thing){
  // Transposed from base/parts/storage/common/storage.ts
  ^{ name: "isStorageItemsChangeEvent", status: "pure" };
}

!createMigratingStorage(...args){
  ^{ className: "MigratingStorage", methods: ["init", "setFallbackStorage", "get", "if", "loadMigratedKeys", "if", "persistMigratedKeys"] };
}

!getStorageHintEnum(){
  ^{ enumName: "StorageHint", members: ["// A hint to the storage that the storage
	// does not exist on disk yet. This allows
	// the storage library to improve startup
	// time by not checking the storage for data.
	STORAGE_DOES_NOT_EXIST", "// A hint to the storage that the storage
	// is backed by an in-memory storage.
	STORAGE_IN_MEMORY"] };
}

!getStorageStateEnum(){
  ^{ enumName: "StorageState", members: ["None", "Initialized", "Closed"] };
}

=ex{isStorageItemsChangeEvent,createMigratingStorage,getStorageHintEnum,getStorageStateEnum}
