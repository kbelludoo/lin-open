@LIN:L1c:0.2
!createClient(type,props){
    this.type = type;
    Object.assign(this, props);
  }
!peek(){
      ^tokens[pos] || { type: 'EOF', value: '' };
    }
!peekNext(){
      ^tokens[pos + 1] || { type: 'EOF', value: '' };
    }
!next(){
      ^tokens[pos++];
    }
!match(value){
      ?(peek().value === value){
        ^next();
      }
      ^null;
    }
!expect(value){
      const tok = next();
      ?(!tok || tok.value !== value){
        throw new Error(`Expected '${value}', got '${tok ? tok.value : 'EOF'}' at pos ${pos}`);
      }
      ^tok;
    }
!parseFunction(isExported){
      match('async');
      expect('function');
      let id = null;
      ?(peek().type === 'IDENTIFIER'){
        id = next().value;
      }
      ?(peek().value === '<'){
        next();
        let angleDepth = 1;
        while (angleDepth > 0 && peek().type !== 'EOF') {
          ?(peek().value === '<'){ angleDepth++; }
          else ?(peek().value === '>'){ angleDepth--; }
          next();
        }
      }
      expect('(');
      const params = [];
      while ((peek().type !== 'PUNCTUATOR' || peek().value !== ')') && peek().type !== 'EOF') {
        const pBefore = pos;
        ?(match('...')){}
        ?(peek().type === 'IDENTIFIER'){
          params.push(next().value);
        }
        ?(peek().value === '!'){ next(); }
        ?(peek().value === '?'){ next(); }
        ?(peek().value === ':'){
          next();
          let parenDepth = 0;
          let braceDepth = 0;
          let bracketDepth = 0;
          let angleDepth = 0;
          while (peek().type !== 'EOF') {
            const pv = peek().value;
            ?(parenDepth === 0 && braceDepth === 0 && bracketDepth === 0 && angleDepth === 0 && (pv === '=' || pv === ',' || pv === ')'){ ) {
              break; }
            }
            ?(pv === '(') parenDepth++;
            else if (pv === ')'){ parenDepth--; }:(pv === '{'){ braceDepth++; }:(pv === '}'){ braceDepth--; }:(pv === '['){ bracketDepth++; }:(pv === ']'){ bracketDepth--; }:(pv === '<'){ angleDepth++; }:(pv === '>'){ angleDepth--; }
            next();
          }
        }
        ?(peek().value === '='){
          next();
          parseAssignment();
        }
        ?(peek().value === ','){ next(); }
        ?(pos === pBefore){
          throw new Error(`Parser stuck in function params at pos ${pos}: ${JSON.stringify(peek())}`);
        }
      }
      expect(')');
      ?(peek().value === ':'){
        next();
        let parenDepth = 0;
        let braceDepth = 0;
        let bracketDepth = 0;
        let angleDepth = 0;
        while (peek().type !== 'EOF') {
          const pv = peek().value;
          ?(parenDepth === 0 && braceDepth === 0 && bracketDepth === 0 && angleDepth === 0 && (pv === '{' || pv === ';')){
            break;
          }
          ?(pv === '(') parenDepth++;
          else if (pv === ')'){ parenDepth--; }:(pv === '{'){ braceDepth++; }:(pv === '}'){ braceDepth--; }:(pv === '['){ bracketDepth++; }:(pv === ']'){ bracketDepth--; }:(pv === '<'){ angleDepth++; }:(pv === '>'){ angleDepth--; }
          next();
        }
      }
      ?(match(';')){
        ^new AstNode('EmptyStatement');
      }
      const body = parseBlockStatement();
      ^new AstNode('FunctionDeclaration', { id, params, body, isExported });
    }

    function parseBlockStatement() {
      expect('{');
      const body = [];
      while ((peek().type !== 'PUNCTUATOR' || peek().value !== '}') && peek().type !== 'EOF') {
        const pBefore = pos;
        body.push(parseStatement());
        ?(pos === pBefore){
          throw new Error(`Parser stuck in block at pos ${pos}: ${JSON.stringify(peek())}`);
        }
      }
      expect('}');
      ^new AstNode('BlockStatement', { body });
    }
!parseBlockStatement(){
      expect('{');
      const body = [];
      while ((peek().type !== 'PUNCTUATOR' || peek().value !== '}') && peek().type !== 'EOF') {
        const pBefore = pos;
        body.push(parseStatement());
        ?(pos === pBefore){
          throw new Error(`Parser stuck in block at pos ${pos}: ${JSON.stringify(peek())}`);
        }
      }
      expect('}
!parseStatement(){
      const tok = peek();

      ?(tok.value === 'import'){
        next();
        ?(peek().value === 'type'){ next(); }
        ?(peek().value === '{'){
          next();
          const specifiers = [];
          while (peek().value !== '}' && peek().type !== 'EOF') {
            ?(peek().type === 'IDENTIFIER'){ specifiers.push(next().value); }
            ?(peek().value === ','){ next(); }
          }
          expect('}');
          ?(match('from')){
            const src = next().value;
            match(';');
            ^new AstNode('ImportDeclaration', { specifiers, source: src });
          }
        }:{
          while (peek().value !== ';' && peek().type !== 'EOF') next();
          match(';');
          ^new AstNode('EmptyStatement');
        }
      }
!parseVariableDeclaration(isExported){
      const kind = next().value;
      const declarations = [];
      while (peek().type === 'IDENTIFIER' || peek().value === '[' || peek().value === '{') {
        let id;
        ?(peek().value === '['){
          next();
          const items = [];
          while (peek().value !== ']' && peek().type !== 'EOF') {
            ?(peek().type === 'IDENTIFIER'){ items.push(next().value); }
            ?(peek().value === ','){ next(); }
          }
          expect(']');
          id = items;
        } else ?(peek().value === '{'){
          next();
          const items = [];
          while (peek().value !== '}' && peek().type !== 'EOF') {
            ?(peek().type === 'IDENTIFIER'){ items.push(next().value); }
            ?(peek().value === ','){ next(); }
          }
          expect('}');
          id = items;
        }:{
          id = next().value;
        }
        ?(peek().value === '!'){ next(); }
        ?(peek().value === ':'){
          next();
          let parenDepth = 0;
          let braceDepth = 0;
          let bracketDepth = 0;
          let angleDepth = 0;
          while (peek().type !== 'EOF') {
            const pv = peek().value;
            ?(parenDepth === 0 && braceDepth === 0 && bracketDepth === 0 && angleDepth === 0 && (pv === '=' || pv === ',' || pv === ';' || pv === '}' || pv === ')'){ ) {
              break; }
            }
            ?(pv === '(') parenDepth++;
            else if (pv === ')'){ parenDepth--; }:(pv === '{'){ braceDepth++; }:(pv === '}'){ braceDepth--; }:(pv === '['){ bracketDepth++; }:(pv === ']'){ bracketDepth--; }:(pv === '<'){ angleDepth++; }:(pv === '>'){ angleDepth--; }
            next();
          }
        }
        let init = null;
        ?(match('=')){
          init = parseAssignment();
        }
        declarations.push(new AstNode('VariableDeclarator', { id, init }));
        ?(!match(',')){ break; }
      }
!parseExpression(){
      const exprs = [parseAssignment()];
      while (match(',')) {
        exprs.push(parseAssignment());
      }
      ^exprs.length === 1 ? exprs[0] : new AstNode('SequenceExpression', { expressions: exprs });
    }
!parseAssignment(){
      const left = parseConditional();
      const tok = peek();
      ?(tok.type === 'PUNCTUATOR' && ['=', '+=', '-=', '*=', '/=', '%=', '>>>=', '>>=', '<<=', '&=', '|=', '^='].includes(tok.value)){
        const op = next().value;
        const right = parseAssignment();
        ^new AstNode('AssignmentExpression', { operator: op, left, right });
      }
      ^left;
    }
!parseConditional(){
      const expr = parseBinary(0);
      ?(match('?')){
        const consequent = parseExpression();
        expect(':');
        const alternate = parseConditional();
        ^new AstNode('ConditionalExpression', { test: expr, consequent, alternate });
      }
      ^expr;
    }
!parseBinary(minPrec){
      let left = parsePrimary();

      while ((peek().type === 'PUNCTUATOR' || peek().type === 'IDENTIFIER') && isBinaryOp(peek().value)) {
        const op = peek().value;
        const prec = getPrecedence(op);
        ?(prec < minPrec){ break; }
        next();
        const right = parseBinary(prec + 1);
        left = new AstNode('BinaryExpression', { operator: op, left, right });
      }

      ^left;
    }
!isBinaryOp(op){
      ^['===', '!==', '==', '!=', '<=', '>=', '<', '>', '+', '-', '*', '/', '%', '&&', '||', '||', '**', '^', '&', '|', '>>>', '>>', '<<', 'instanceof', 'in'].includes(op);
    }
!getPrecedence(op){
      ?(op === '||' || op === '||'){ ^1; }
      ?(op === '&&'){ ^2; }
      ?(['|', '^', '&'].includes(op)){ ^3; }
      ?(['===', '!==', '==', '!='].includes(op)){ ^4; }
      ?(['<', '>', '<=', '>=', 'instanceof', 'in'].includes(op)){ ^5; }
      ?(['>>>', '>>', '<<'].includes(op)){ ^6; }
      ?(op === '+' || op === '-'){ ^7; }
      ?(op === '*' || op === '/' || op === '%' || op === '**'){ ^8; }
      ^0;
    }
!parsePrimary(){
      const tok = peek();
      let node = null;

      ?(tok.value === 'new'){
        next();
        let callee = null;
        ?(peek().type === 'IDENTIFIER'){
          callee = new AstNode('Identifier', { name: next().value });
        }:{
          callee = parsePrimary();
        }
        ?(peek().value === '<'){
          next();
          let angleDepth = 1;
          while (angleDepth > 0 && peek().type !== 'EOF') {
            ?(peek().value === '<'){ angleDepth++; }
            else ?(peek().value === '>'){ angleDepth--; }
            next();
          }
        }
        let args = [];
        ?(match('(')) {
          while ((peek().type !== 'PUNCTUATOR' || peek().value !== ')') && peek().type !== 'EOF'){
            args.push(parseAssignment());
            ?(!match(',')){ break; }
          }
          expect(')');
        }
        ^new AstNode('NewExpression', { callee, arguments: args });
      }

      ?(tok.value === '...' ){
        next();
        const arg = parsePrimary();
        ^new AstNode('SpreadElement', { argument: arg });
      }

      ?(tok.type === 'STRING_LITERAL'){
        next();
        node = new AstNode('Literal', { value: tok.value, raw: tok.raw || JSON.stringify(tok.value) });
        ^node;
      }
      ?(tok.type === 'NUMERIC_LITERAL'){
        next();
        node = new AstNode('Literal', { value: Number(tok.value), raw: tok.value });
        ^node;
      }
      ?(tok.type === 'BOOLEAN_LITERAL'){
        next();
        node = new AstNode('Literal', { value: tok.value === 'true', raw: tok.value });
        ^node;
      }
      ?(tok.type === 'NULL_LITERAL'){
        next();
        node = new AstNode('Literal', { value: null, raw: 'null' });
        ^node;
      }
      ?(tok.type === 'REGEXP_LITERAL'){
        next();
        node = new AstNode('Literal', { value: tok.value, raw: tok.value, regex: { pattern: tok.pattern, flags: tok.flags } });
        ^node;
      }

      ?(tok.type === 'PUNCTUATOR' && (tok.value === '++' || tok.value === '--')){
        const op = next().value;
        const arg = parsePrimary();
        ^new AstNode('UpdateExpression', { operator: op, argument: arg, prefix: true });
      }

      ?((tok.type === 'PUNCTUATOR' && ['!', '-', '+', '~'].includes(tok.value)) || (tok.type === 'IDENTIFIER' && ['typeof', 'delete'].includes(tok.value))){
        const op = next().value;
        const arg = parsePrimary();
        ^new AstNode('UnaryExpression', { operator: op, argument: arg, prefix: true });
      }

      ?(tok.type === 'IDENTIFIER' && tok.value === 'function'){
        node = parseFunction();
      }:(tok.type === 'PUNCTUATOR' && tok.value === '['){
        next();
        const elements = [];
        while (peek().value !== ']' && peek().type !== 'EOF') {
          elements.push(parseAssignment());
          ?(!match(',')){ break; }
        }
        expect(']');
        node = new AstNode('ArrayExpression', { elements });
      }:(tok.type === 'PUNCTUATOR' && tok.value === '{'){
        next();
        const properties = [];
        while ((peek().type !== 'PUNCTUATOR' || peek().value !== '}') && peek().type !== 'EOF') {
          ?(peek().type === 'PUNCTUATOR' && peek().value === '...'){
            next();
            const arg = parseAssignment();
            properties.push(new AstNode('SpreadElement', { argument: arg }));
            ?(!match(',')){ break; }
            continue;
          }
          let key;
          let computed = false;
          ?(peek().type === 'PUNCTUATOR' && peek().value === '['){
            next();
            key = parseAssignment();
            expect(']');
            computed = true;
          }:{
            const keyTok = next();
            ?(keyTok.type === 'STRING_LITERAL' || keyTok.type === 'NUMERIC_LITERAL'){
              key = new AstNode('Literal', { value: keyTok.value, raw: keyTok.raw || JSON.stringify(keyTok.value) });
            }:{
              key = new AstNode('Identifier', { name: keyTok.value });
            }
          }
          let val = null;
          ?(match('(')) {
            const params = [];
            while (peek().value !== ')' && peek().type !== 'EOF'){
              ?(peek().type === 'IDENTIFIER'){ params.push(next().value); }
              ?(peek().value === ','){ next(); }
            }
            expect(')');
            const mBody = parseBlockStatement();
            val = new AstNode('FunctionDeclaration', { id: key.name || 'method', params, body: mBody });
          } else ?(match(':')){
            val = parseAssignment();
          }:{
            val = key;
          }
          properties.push(new AstNode('Property', { key: key.name || key.value || key, value: val, computed }));
          ?(!match(',')){ break; }
        }
        expect('}');
        node = new AstNode('ObjectExpression', { properties });
      }
!extractSymbolsAndCallsFromAst(ast,sourceOrigin){
  const functions = new Map();
  const enums = new Map();
  const constants = new Map();
  const directCalls = [];
  const methodCalls = [];

  function visit(node, currentFnName = null) {
    ?(!node || typeof node !== 'object'){ ^null; }

    ?(node.type === 'EnumDeclaration'){
      enums.set(node.id, { name: node.id, node, origin: sourceOrigin });
      ^null;
    }

    ?(node.type === 'FunctionDeclaration'){
      const fnName = node.id || currentFnName;
      ?(fnName && currentFnName === null){
        functions.set(fnName, {
          name: fnName,
          params: node.params,
          node,
          origin: sourceOrigin,
          calls: []
        });
      }
      visitNodeChildren(node.body, fnName);
      ^null;
    }

    ?(node.type === 'VariableDeclaration'){
      for (const decl of node.declarations) {
        ?(decl.init && (decl.init.type === 'ArrowFunctionExpression' || decl.init.type === 'FunctionDeclaration')){
          const fnName = decl.id;
          ?(typeof fnName === 'string' && currentFnName === null){
            functions.set(fnName, {
              name: fnName,
              params: decl.init.params,
              node: decl.init,
              origin: sourceOrigin,
              calls: []
            });
          }
          visitNodeChildren(decl.init.body, fnName);
        }:{
          ?(currentFnName === null && typeof decl.id === 'string'){
            constants.set(decl.id, { name: decl.id, node: decl, origin: sourceOrigin });
          }
          visit(decl.init, currentFnName);
        }
      }
      ^null;
    }

    ?(node.type === 'Property' && node.value && (node.value.type === 'FunctionDeclaration' || node.value.type === 'ArrowFunctionExpression')){
      const fnName = node.key;
      functions.set(fnName, {
        name: fnName,
        params: node.value.params,
        node: node.value,
        origin: sourceOrigin,
        calls: []
      });
      visitNodeChildren(node.value.body, fnName);
      ^null;
    }

    ?(node.type === 'CallExpression'){
      ?(node.callee.type === 'Identifier'){
        const callInfo = { kind: 'direct', name: node.callee.name, args: node.arguments };
        directCalls.push(callInfo);
        ?(currentFnName && functions.has(currentFnName)){
          functions.get(currentFnName).calls.push(callInfo);
        }
      }:(node.callee.type === 'MemberExpression'){
        const recvName = extractReceiverName(node.callee.object);
        const methodName = node.callee.property;
        const fullSymbol = recvName ? `${recvName}.${methodName}` : methodName;
        const callInfo = { kind: 'method', receiver: recvName, method: methodName, fullSymbol, args: node.arguments };
        methodCalls.push(callInfo);
        ?(currentFnName && functions.has(currentFnName)){
          functions.get(currentFnName).calls.push(callInfo);
        }
      }
    }

    visitNodeChildren(node, currentFnName);
  }

  function visitNodeChildren(node, currentFnName) {
    for (const key of Object.keys(node)) {
      ?(key === 'type'){ continue; }
      const child = node[key];
      ?(Array.isArray(child)){
        for (const item of child) {
          ?(item && typeof item === 'object'){ visit(item, currentFnName); }
        }
      }:(child && typeof child === 'object'){
        visit(child, currentFnName);
      }
    }
  }

  visit(ast);
  ^{ functions, enums, constants, directCalls, methodCalls };
}
!visit(node,currentFnName){
    ?(!node || typeof node !== 'object'){ ^null; }

    ?(node.type === 'EnumDeclaration'){
      enums.set(node.id, { name: node.id, node, origin: sourceOrigin });
      ^null;
    }

    ?(node.type === 'FunctionDeclaration'){
      const fnName = node.id || currentFnName;
      ?(fnName && currentFnName === null){
        functions.set(fnName, {
          name: fnName,
          params: node.params,
          node,
          origin: sourceOrigin,
          calls: []
        });
      }
      visitNodeChildren(node.body, fnName);
      ^null;
    }

    ?(node.type === 'VariableDeclaration'){
      for (const decl of node.declarations) {
        ?(decl.init && (decl.init.type === 'ArrowFunctionExpression' || decl.init.type === 'FunctionDeclaration')){
          const fnName = decl.id;
          ?(typeof fnName === 'string' && currentFnName === null){
            functions.set(fnName, {
              name: fnName,
              params: decl.init.params,
              node: decl.init,
              origin: sourceOrigin,
              calls: []
            });
          }
          visitNodeChildren(decl.init.body, fnName);
        }:{
          ?(currentFnName === null && typeof decl.id === 'string'){
            constants.set(decl.id, { name: decl.id, node: decl, origin: sourceOrigin });
          }
          visit(decl.init, currentFnName);
        }
      }
      ^null;
    }

    ?(node.type === 'Property' && node.value && (node.value.type === 'FunctionDeclaration' || node.value.type === 'ArrowFunctionExpression')){
      const fnName = node.key;
      functions.set(fnName, {
        name: fnName,
        params: node.value.params,
        node: node.value,
        origin: sourceOrigin,
        calls: []
      });
      visitNodeChildren(node.value.body, fnName);
      ^null;
    }

    ?(node.type === 'CallExpression'){
      ?(node.callee.type === 'Identifier'){
        const callInfo = { kind: 'direct', name: node.callee.name, args: node.arguments };
        directCalls.push(callInfo);
        ?(currentFnName && functions.has(currentFnName)){
          functions.get(currentFnName).calls.push(callInfo);
        }
      }:(node.callee.type === 'MemberExpression'){
        const recvName = extractReceiverName(node.callee.object);
        const methodName = node.callee.property;
        const fullSymbol = recvName ? `${recvName}.${methodName}` : methodName;
        const callInfo = { kind: 'method', receiver: recvName, method: methodName, fullSymbol, args: node.arguments };
        methodCalls.push(callInfo);
        ?(currentFnName && functions.has(currentFnName)){
          functions.get(currentFnName).calls.push(callInfo);
        }
      }
    }

    visitNodeChildren(node, currentFnName);
  }
!visitNodeChildren(node,currentFnName){
    for (const key of Object.keys(node)) {
      ?(key === 'type'){ continue; }
      const child = node[key];
      ?(Array.isArray(child)){
        for (const item of child) {
          ?(item && typeof item === 'object'){ visit(item, currentFnName); }
        }
      }:(child && typeof child === 'object'){
        visit(child, currentFnName);
      }
    }
  }
!extractReceiverName(node){
  ?(!node){ ^''; }
  ?(node.type === 'Identifier'){ ^node.name; }
  ?(node.type === 'MemberExpression'){
    const parent = extractReceiverName(node.object);
    ^parent ? `${parent}.${node.property}` : node.property;
  }
  ^'';
}
!countSemanticUnits(node){
  const units = {
    CALL: 0,
    BRANCH: 0,
    LOOP: 0,
    PROPERTY_READ: 0,
    PROPERTY_WRITE: 0,
    ALLOCATION: 0,
    ARITHMETIC: 0,
    LOGICAL: 0,
    CLOSURE: 0,
    VARIABLE: 0,
    RETURN: 0,
    total: 0
  };

  function count(n) {
    ?(!n || typeof n !== 'object'){ ^null; }

    switch (n.type) {
      case 'CallExpression': units.CALL++; break;
      case 'IfStatement':
      case 'ConditionalExpression': units.BRANCH++; break;
      case 'ForStatement':
      case 'WhileStatement': units.LOOP++; break;
      case 'MemberExpression': units.PROPERTY_READ++; break;
      case 'AssignmentExpression':
        ?(n.left.type === 'MemberExpression'){ units.PROPERTY_WRITE++; }
        else units.VARIABLE++;
        break;
      case 'ArrayExpression':
      case 'ObjectExpression': units.ALLOCATION++; break;
      case 'BinaryExpression':
        ?(['+', '-', '*', '/', '%', '**'].includes(n.operator)){ units.ARITHMETIC++; }
        else ?(['&&', '||', '||'].includes(n.operator)){ units.LOGICAL++; }
        else units.BRANCH++;
        break;
      case 'UnaryExpression':
        ?(n.operator === '!'){ units.LOGICAL++; }
        else units.ARITHMETIC++;
        break;
      case 'FunctionDeclaration':
      case 'ArrowFunctionExpression': units.CLOSURE++; break;
      case 'VariableDeclaration': units.VARIABLE += (n.declarations ? n.declarations.length : 1); break;
      case 'ReturnStatement': units.RETURN++; break;
    }
    units.total++;

    for (const k of Object.keys(n)) {
      ?(k === 'type'){ continue; }
      const child = n[k];
      ?(Array.isArray(child)){
        for (const item of child) ?(item && typeof item === 'object'){ count(item); }
      }:(child && typeof child === 'object'){
        count(child);
      }
    }
  }

  count(node);
  ^units;
}
!count(n){
    ?(!n || typeof n !== 'object'){ ^null; }

    switch (n.type) {
      case 'CallExpression': units.CALL++; break;
      case 'IfStatement':
      case 'ConditionalExpression': units.BRANCH++; break;
      case 'ForStatement':
      case 'WhileStatement': units.LOOP++; break;
      case 'MemberExpression': units.PROPERTY_READ++; break;
      case 'AssignmentExpression':
        ?(n.left.type === 'MemberExpression'){ units.PROPERTY_WRITE++; }
        else units.VARIABLE++;
        break;
      case 'ArrayExpression':
      case 'ObjectExpression': units.ALLOCATION++; break;
      case 'BinaryExpression':
        ?(['+', '-', '*', '/', '%', '**'].includes(n.operator)){ units.ARITHMETIC++; }
        else ?(['&&', '||', '||'].includes(n.operator)){ units.LOGICAL++; }
        else units.BRANCH++;
        break;
      case 'UnaryExpression':
        ?(n.operator === '!'){ units.LOGICAL++; }
        else units.ARITHMETIC++;
        break;
      case 'FunctionDeclaration':
      case 'ArrowFunctionExpression': units.CLOSURE++; break;
      case 'VariableDeclaration': units.VARIABLE += (n.declarations ? n.declarations.length : 1); break;
      case 'ReturnStatement': units.RETURN++; break;
    }
    units.total++;

    for (const k of Object.keys(n)) {
      ?(k === 'type'){ continue; }
      const child = n[k];
      ?(Array.isArray(child)){
        for (const item of child) ?(item && typeof item === 'object'){ count(item); }
      }:(child && typeof child === 'object'){
        count(child);
      }
    }
  }
!computeAstTransitiveClosure(entrypoints,symbolPool){
  const closure = new Map();
  const queue = [...entrypoints];
  const visited = new Set();

  while (queue.length > 0) {
    const current = queue.shift();
    ?(visited.has(current)){ continue; }
    visited.add(current);

    const fnDef = symbolPool.get(current);
    ?(fnDef){
      closure.set(current, fnDef);

      for (const call of fnDef.calls) {
        ?(call.kind === 'direct' && symbolPool.has(call.name)){
          ?(!visited.has(call.name)){ queue.push(call.name); }
        }
      }
    }
  }

  ^closure;
}
!runSemanticClosurePipeline(userSource,opts){
  const libSources = opts.libraries || {};
  const entrypoints = opts.entrypoints || [];

  const userAst = TrueAstParser.parse(userSource);
  const userSymbols = extractSymbolsAndCallsFromAst(userAst, TAXONOMY.USER_DEFINED);

  const libSymbols = new Map();
  const libEnums = new Map();
  const libConstants = new Map();

  for (const [libName, libCode] of Object.entries(libSources)) {
    const libAst = TrueAstParser.parse(libCode);
    const symbols = extractSymbolsAndCallsFromAst(libAst, TAXONOMY.LIBRARY_SOURCE);
    for (const [k, v] of symbols.functions) {
      libSymbols.set(k, v);
    }
    for (const [k, v] of symbols.enums) {
      libEnums.set(k, v);
    }
    for (const [k, v] of symbols.constants) {
      libConstants.set(k, v);
    }
  }

  const allEnums = new Map([...libEnums, ...userSymbols.enums]);
  const allConstants = new Map([...libConstants, ...userSymbols.constants]);

  const symbolPool = new Map([...libSymbols, ...userSymbols.functions]);

  const topCalls = userSymbols.directCalls.map(c => c.name).filter(n => symbolPool.has(n));
  const effectiveEntrypoints = Array.from(new Set([
    ...(entrypoints.length > 0 ? entrypoints : Array.from(userSymbols.functions.keys())),
    ...topCalls
  ]));

  const closure = computeAstTransitiveClosure(effectiveEntrypoints, symbolPool);

  const resolved = new Set();
  const materialized = new Set();
  const host_required = new Set();
  const unresolved = new Set();

  let materializedSemanticUnits = 0;
  let totalRequiredSemanticUnits = 0;

  for (const [name, fnDef] of closure.entries()) {
    resolved.add(name);
    materialized.add(name);

    const fnUnits = countSemanticUnits(fnDef.node);
    materializedSemanticUnits += fnUnits.total;
    totalRequiredSemanticUnits += fnUnits.total;

    for (const call of fnDef.calls) {
      ?(call.kind === 'direct'){
        ?(closure.has(call.name) || symbolPool.has(call.name)){
          resolved.add(call.name);
          materialized.add(call.name);
        } else ?(HOST_CAPABILITY_REGISTRY.has(call.name)){
          resolved.add(call.name);
          host_required.add(call.name);
          totalRequiredSemanticUnits += 1;
        }:{
          unresolved.add(call.name);
          totalRequiredSemanticUnits += 1;
        }
      }:(call.kind === 'method'){
        ?(HOST_CAPABILITY_REGISTRY.has(call.fullSymbol) || HOST_CAPABILITY_REGISTRY.has(call.receiver)){
          resolved.add(call.fullSymbol);
          host_required.add(call.fullSymbol);
          totalRequiredSemanticUnits += 1;
        } else ?(BUILTIN_PURE_METHODS.has(call.method)){
          resolved.add(call.method);
          materialized.add(`specialized:${call.method}`);
        } else ?(closure.has(call.method) || symbolPool.has(call.method)){
          resolved.add(call.method);
          materialized.add(call.method);
        }:{
          unresolved.add(call.fullSymbol);
          totalRequiredSemanticUnits += 1;
        }
      }
    }
  }

  const symbolDenominator = materialized.size + host_required.size + unresolved.size;
  const symbol_coverage = symbolDenominator === 0 ? 1.0 : materialized.size / symbolDenominator;
  const semantic_coverage = totalRequiredSemanticUnits === 0 ? 1.0 : materializedSemanticUnits / totalRequiredSemanticUnits;

  const userFnsLin = [];
  const runtimeFnsLin = [];

  for (const [name, fnDef] of closure.entries()) {
    const fnLin = LinAstEmitter.emitFunction(fnDef);
    ?(fnDef.origin === TAXONOMY.USER_DEFINED){
      userFnsLin.push(fnLin);
    }:{
      runtimeFnsLin.push(fnLin);
    }
  }

  const allEnumsLin = [];
  for (const [name, enumDef] of allEnums.entries()) {
    const variants = enumDef.node.members.map(m => {
      const valStr = LinAstEmitter.emitExpression(m.value);
      ^`  ${m.name}: ${valStr}`;
    }).join(',\n');
    const enumLin = `enum ${name} {\n${variants}\n}`;
    allEnumsLin.push(enumLin);
  }

  const userConsts = [];
  const runtimeConsts = [];
  for (const [name, constDef] of allConstants.entries()) {
    const valStr = LinAstEmitter.emitExpression(constDef.node.init);
    const constLine = `var ${name} = ${valStr};`;
    ?(constDef.origin === TAXONOMY.USER_DEFINED){ userConsts.push(constLine); }
    else runtimeConsts.push(constLine);
  }

  const program_lin = formatLinModule(userFnsLin, effectiveEntrypoints, allEnumsLin, userConsts);
  const extracted_runtime_lin = formatLinModule(runtimeFnsLin, Array.from(libSymbols.keys()), allEnumsLin, runtimeConsts);

  ^{
    program_lin,
    extracted_runtime_lin,
    capability_manifest: {
      contract: 'LIN_CAPABILITY_V1',
      resolved: Array.from(resolved),
      materialized: Array.from(materialized),
      host_required: Array.from(host_required),
      unresolved: Array.from(unresolved),
      symbol_coverage: parseFloat(symbol_coverage.toFixed(4)),
      semantic_coverage: parseFloat(semantic_coverage.toFixed(4)),
      materialized_semantic_units: materializedSemanticUnits,
      total_required_semantic_units: totalRequiredSemanticUnits
    },
    closure_size: closure.size,
    coverage: parseFloat(semantic_coverage.toFixed(4))
  };
}
!formatLinModule(fnStrings,exports,enumsStrings,constsList){
  ?(fnStrings.length === 0 && enumsStrings.length === 0 && constsList.length === 0){
    ^`@LIN:L1c:0.2\n^schema_once ^lossy=true\n~G{?=?(enumsStrings.length > 0){
    parts.push('');
    parts.push(enumsStrings.join('\n\n'));
  }

  ?(constsList.length > 0){
    parts.push('');
    parts.push(constsList.join('\n'));
  }

  ?(fnStrings.length > 0){
    parts.push('');
    parts.push(fnStrings.join('\n\n'));
  }

  ?(exports.length > 0){
    parts.push('');
    parts.push(`=ex{${exports.join(', ')}}`);
  }

  ^parts.join('\n');
}
!isolateFailureCause(testCase,oracleResult,linResult,manifest){
  ?(manifest.unresolved.length > 0){
    ^{
      fault: 'CAPABILITY',
      reason: `Unresolved external symbols present: ${manifest.unresolved.join(', ')}`
    };
  }
  ?(manifest.host_required.length > 0 && (linResult === null || linResult === undefined)){
    ^{
      fault: 'CAPABILITY',
      reason: `Required host capability boundary: ${manifest.host_required.join(', ')}`
    };
  }
  ?(typeof linResult === 'string' && linResult.startsWith('__RUNTIME_ERROR__')){
    ^{
      fault: 'RUNTIME',
      reason: `Rust native runtime raised error executing lowered bytecode`
    };
  }
  ^{
    fault: 'LOWERING',
    reason: `Output mismatch between JS oracle and lowered LIN logic`
  };
}
=ex{createClient, peek, peekNext, next, match, expect, parseFunction, parseBlockStatement, parseStatement, parseVariableDeclaration, parseExpression, parseAssignment, parseConditional, parseBinary, isBinaryOp, getPrecedence, parsePrimary, extractSymbolsAndCallsFromAst, visit, visitNodeChildren, extractReceiverName, countSemanticUnits, count, computeAstTransitiveClosure, runSemanticClosurePipeline, formatLinModule, isolateFailureCause}