@LIN:L1c:0.2
~G{?=if #=for ^=ret :else %=contract *=effect}

~Buffer {
  %(inv: self.size >= 0 && self.size <= self.capacity)
  *(HeapMut)

  !corruptPush(self, item)
  {
    self.size = self.capacity + 5;
    ^self.size
  }
}
