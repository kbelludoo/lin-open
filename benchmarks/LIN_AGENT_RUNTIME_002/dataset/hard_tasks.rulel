@RULEL:HARD_TASKS:1.0.0
~DATA
  .task_id="HARD_T01" type="edge_case" title="[#1] Handle leading whitespace and optional leading 'v' with build metadata" file="src/parser.js" symbol="parseVersion" bug_find="const regex = /^v?(\\d+)\\.(\\d+)\\.(\\d+)(?:-([0-9A-Za-z.-]+))?(?:\\+([0-9A-Za-z.-]+))?$/;" bug_replace="const regex = /^(\\d+)\\.(\\d+)\\.(\\d+)$/;" instruction="The regex in parseVersion fails on versions with leading 'v', prerelease identifiers, and build metadata (e.g. 'v1.2.3-alpha.1+build.123'). Fix parseVersion regex to support optional 'v', prerelease tag, and build tag."
  .task_id="HARD_T02" type="unit_bug" title="[#2] Numeric vs alphanumeric prerelease identifier precedence" file="src/comparator.js" symbol="compareIdentifiers" bug_find="if (anum && bnum) {\n    const na = parseInt(a, 10);\n    const nb = parseInt(b, 10);\n    return na === nb ? 0 : na > nb ? 1 : -1;\n  }" bug_replace="if (anum && bnum) {\n    return a.length - b.length;\n  }" instruction="In compareIdentifiers (src/comparator.js), when both identifiers are numeric, they must be compared by integer value (parseInt). Fix compareIdentifiers to return 0 for equal, 1 if a > b, -1 if a < b."
  .task_id="HARD_T03" type="multi_module_regression" title="[#3] Tilde range minor bump boundary for major.minor.patch" file="src/ranges.js" symbol="parseRange" bug_find="return [{ op: '>=', version: clean.slice(1) }, { op: '<', version: `${base.major}.${base.minor + 1}.0` }];" bug_replace="return [{ op: '>=', version: clean.slice(1) }, { op: '<=', version: `${base.major}.${base.minor + 1}.0` }];" instruction="In parseRange (src/ranges.js), tilde range '~1.2.3' must strictly bound to '< 1.3.0', but currently uses '<=' allowing 1.3.0 to leak. Fix upper bound operator to strict '<'."
  .task_id="HARD_T04" type="multi_module_regression" title="[#4] maxSatisfying returning empty string on non-satisfaction" file="src/satisfier.js" symbol="maxSatisfying" bug_find="return max;" bug_replace="return max || '';" instruction="maxSatisfying in src/satisfier.js must return null (not empty string or undefined) when no version satisfies the range. Ensure it returns null if no valid version was found."
  .task_id="HARD_T05" type="edge_case" title="[#5] Strict gt() must return false for equal version inputs" file="src/comparator.js" symbol="gt" bug_find="return c !== null && c > 0;" bug_replace="return c !== null && c >= 0;" instruction="gt(v1, v2) in src/comparator.js is returning true when versions are equal. Fix gt to return true strictly when compareVersions(v1, v2) > 0."
  .task_id="HARD_T06" type="edge_case" title="[#6] Handle leading whitespace and optional leading 'v' with build metadata" file="src/parser.js" symbol="parseVersion" bug_find="const regex = /^v?(\\d+)\\.(\\d+)\\.(\\d+)(?:-([0-9A-Za-z.-]+))?(?:\\+([0-9A-Za-z.-]+))?$/;" bug_replace="const regex = /^(\\d+)\\.(\\d+)\\.(\\d+)$/;" instruction="The regex in parseVersion fails on versions with leading 'v', prerelease identifiers, and build metadata (e.g. 'v1.2.3-alpha.1+build.123'). Fix parseVersion regex to support optional 'v', prerelease tag, and build tag."
  .task_id="HARD_T07" type="unit_bug" title="[#7] Numeric vs alphanumeric prerelease identifier precedence" file="src/comparator.js" symbol="compareIdentifiers" bug_find="if (anum && bnum) {\n    const na = parseInt(a, 10);\n    const nb = parseInt(b, 10);\n    return na === nb ? 0 : na > nb ? 1 : -1;\n  }" bug_replace="if (anum && bnum) {\n    return a.length - b.length;\n  }" instruction="In compareIdentifiers (src/comparator.js), when both identifiers are numeric, they must be compared by integer value (parseInt). Fix compareIdentifiers to return 0 for equal, 1 if a > b, -1 if a < b."
  .task_id="HARD_T08" type="multi_module_regression" title="[#8] Tilde range minor bump boundary for major.minor.patch" file="src/ranges.js" symbol="parseRange" bug_find="return [{ op: '>=', version: clean.slice(1) }, { op: '<', version: `${base.major}.${base.minor + 1}.0` }];" bug_replace="return [{ op: '>=', version: clean.slice(1) }, { op: '<=', version: `${base.major}.${base.minor + 1}.0` }];" instruction="In parseRange (src/ranges.js), tilde range '~1.2.3' must strictly bound to '< 1.3.0', but currently uses '<=' allowing 1.3.0 to leak. Fix upper bound operator to strict '<'."
  .task_id="HARD_T09" type="multi_module_regression" title="[#9] maxSatisfying returning empty string on non-satisfaction" file="src/satisfier.js" symbol="maxSatisfying" bug_find="return max;" bug_replace="return max || '';" instruction="maxSatisfying in src/satisfier.js must return null (not empty string or undefined) when no version satisfies the range. Ensure it returns null if no valid version was found."
  .task_id="HARD_T10" type="edge_case" title="[#10] Strict gt() must return false for equal version inputs" file="src/comparator.js" symbol="gt" bug_find="return c !== null && c > 0;" bug_replace="return c !== null && c >= 0;" instruction="gt(v1, v2) in src/comparator.js is returning true when versions are equal. Fix gt to return true strictly when compareVersions(v1, v2) > 0."
  .task_id="HARD_T11" type="edge_case" title="[#11] Handle leading whitespace and optional leading 'v' with build metadata" file="src/parser.js" symbol="parseVersion" bug_find="const regex = /^v?(\\d+)\\.(\\d+)\\.(\\d+)(?:-([0-9A-Za-z.-]+))?(?:\\+([0-9A-Za-z.-]+))?$/;" bug_replace="const regex = /^(\\d+)\\.(\\d+)\\.(\\d+)$/;" instruction="The regex in parseVersion fails on versions with leading 'v', prerelease identifiers, and build metadata (e.g. 'v1.2.3-alpha.1+build.123'). Fix parseVersion regex to support optional 'v', prerelease tag, and build tag."
  .task_id="HARD_T12" type="unit_bug" title="[#12] Numeric vs alphanumeric prerelease identifier precedence" file="src/comparator.js" symbol="compareIdentifiers" bug_find="if (anum && bnum) {\n    const na = parseInt(a, 10);\n    const nb = parseInt(b, 10);\n    return na === nb ? 0 : na > nb ? 1 : -1;\n  }" bug_replace="if (anum && bnum) {\n    return a.length - b.length;\n  }" instruction="In compareIdentifiers (src/comparator.js), when both identifiers are numeric, they must be compared by integer value (parseInt). Fix compareIdentifiers to return 0 for equal, 1 if a > b, -1 if a < b."
  .task_id="HARD_T13" type="multi_module_regression" title="[#13] Tilde range minor bump boundary for major.minor.patch" file="src/ranges.js" symbol="parseRange" bug_find="return [{ op: '>=', version: clean.slice(1) }, { op: '<', version: `${base.major}.${base.minor + 1}.0` }];" bug_replace="return [{ op: '>=', version: clean.slice(1) }, { op: '<=', version: `${base.major}.${base.minor + 1}.0` }];" instruction="In parseRange (src/ranges.js), tilde range '~1.2.3' must strictly bound to '< 1.3.0', but currently uses '<=' allowing 1.3.0 to leak. Fix upper bound operator to strict '<'."
  .task_id="HARD_T14" type="multi_module_regression" title="[#14] maxSatisfying returning empty string on non-satisfaction" file="src/satisfier.js" symbol="maxSatisfying" bug_find="return max;" bug_replace="return max || '';" instruction="maxSatisfying in src/satisfier.js must return null (not empty string or undefined) when no version satisfies the range. Ensure it returns null if no valid version was found."
  .task_id="HARD_T15" type="edge_case" title="[#15] Strict gt() must return false for equal version inputs" file="src/comparator.js" symbol="gt" bug_find="return c !== null && c > 0;" bug_replace="return c !== null && c >= 0;" instruction="gt(v1, v2) in src/comparator.js is returning true when versions are equal. Fix gt to return true strictly when compareVersions(v1, v2) > 0."
  .task_id="HARD_T16" type="edge_case" title="[#16] Handle leading whitespace and optional leading 'v' with build metadata" file="src/parser.js" symbol="parseVersion" bug_find="const regex = /^v?(\\d+)\\.(\\d+)\\.(\\d+)(?:-([0-9A-Za-z.-]+))?(?:\\+([0-9A-Za-z.-]+))?$/;" bug_replace="const regex = /^(\\d+)\\.(\\d+)\\.(\\d+)$/;" instruction="The regex in parseVersion fails on versions with leading 'v', prerelease identifiers, and build metadata (e.g. 'v1.2.3-alpha.1+build.123'). Fix parseVersion regex to support optional 'v', prerelease tag, and build tag."
  .task_id="HARD_T17" type="unit_bug" title="[#17] Numeric vs alphanumeric prerelease identifier precedence" file="src/comparator.js" symbol="compareIdentifiers" bug_find="if (anum && bnum) {\n    const na = parseInt(a, 10);\n    const nb = parseInt(b, 10);\n    return na === nb ? 0 : na > nb ? 1 : -1;\n  }" bug_replace="if (anum && bnum) {\n    return a.length - b.length;\n  }" instruction="In compareIdentifiers (src/comparator.js), when both identifiers are numeric, they must be compared by integer value (parseInt). Fix compareIdentifiers to return 0 for equal, 1 if a > b, -1 if a < b."
  .task_id="HARD_T18" type="multi_module_regression" title="[#18] Tilde range minor bump boundary for major.minor.patch" file="src/ranges.js" symbol="parseRange" bug_find="return [{ op: '>=', version: clean.slice(1) }, { op: '<', version: `${base.major}.${base.minor + 1}.0` }];" bug_replace="return [{ op: '>=', version: clean.slice(1) }, { op: '<=', version: `${base.major}.${base.minor + 1}.0` }];" instruction="In parseRange (src/ranges.js), tilde range '~1.2.3' must strictly bound to '< 1.3.0', but currently uses '<=' allowing 1.3.0 to leak. Fix upper bound operator to strict '<'."
  .task_id="HARD_T19" type="multi_module_regression" title="[#19] maxSatisfying returning empty string on non-satisfaction" file="src/satisfier.js" symbol="maxSatisfying" bug_find="return max;" bug_replace="return max || '';" instruction="maxSatisfying in src/satisfier.js must return null (not empty string or undefined) when no version satisfies the range. Ensure it returns null if no valid version was found."
  .task_id="HARD_T20" type="edge_case" title="[#20] Strict gt() must return false for equal version inputs" file="src/comparator.js" symbol="gt" bug_find="return c !== null && c > 0;" bug_replace="return c !== null && c >= 0;" instruction="gt(v1, v2) in src/comparator.js is returning true when versions are equal. Fix gt to return true strictly when compareVersions(v1, v2) > 0."
  .task_id="HARD_T21" type="edge_case" title="[#21] Handle leading whitespace and optional leading 'v' with build metadata" file="src/parser.js" symbol="parseVersion" bug_find="const regex = /^v?(\\d+)\\.(\\d+)\\.(\\d+)(?:-([0-9A-Za-z.-]+))?(?:\\+([0-9A-Za-z.-]+))?$/;" bug_replace="const regex = /^(\\d+)\\.(\\d+)\\.(\\d+)$/;" instruction="The regex in parseVersion fails on versions with leading 'v', prerelease identifiers, and build metadata (e.g. 'v1.2.3-alpha.1+build.123'). Fix parseVersion regex to support optional 'v', prerelease tag, and build tag."
  .task_id="HARD_T22" type="unit_bug" title="[#22] Numeric vs alphanumeric prerelease identifier precedence" file="src/comparator.js" symbol="compareIdentifiers" bug_find="if (anum && bnum) {\n    const na = parseInt(a, 10);\n    const nb = parseInt(b, 10);\n    return na === nb ? 0 : na > nb ? 1 : -1;\n  }" bug_replace="if (anum && bnum) {\n    return a.length - b.length;\n  }" instruction="In compareIdentifiers (src/comparator.js), when both identifiers are numeric, they must be compared by integer value (parseInt). Fix compareIdentifiers to return 0 for equal, 1 if a > b, -1 if a < b."
  .task_id="HARD_T23" type="multi_module_regression" title="[#23] Tilde range minor bump boundary for major.minor.patch" file="src/ranges.js" symbol="parseRange" bug_find="return [{ op: '>=', version: clean.slice(1) }, { op: '<', version: `${base.major}.${base.minor + 1}.0` }];" bug_replace="return [{ op: '>=', version: clean.slice(1) }, { op: '<=', version: `${base.major}.${base.minor + 1}.0` }];" instruction="In parseRange (src/ranges.js), tilde range '~1.2.3' must strictly bound to '< 1.3.0', but currently uses '<=' allowing 1.3.0 to leak. Fix upper bound operator to strict '<'."
  .task_id="HARD_T24" type="multi_module_regression" title="[#24] maxSatisfying returning empty string on non-satisfaction" file="src/satisfier.js" symbol="maxSatisfying" bug_find="return max;" bug_replace="return max || '';" instruction="maxSatisfying in src/satisfier.js must return null (not empty string or undefined) when no version satisfies the range. Ensure it returns null if no valid version was found."
  .task_id="HARD_T25" type="edge_case" title="[#25] Strict gt() must return false for equal version inputs" file="src/comparator.js" symbol="gt" bug_find="return c !== null && c > 0;" bug_replace="return c !== null && c >= 0;" instruction="gt(v1, v2) in src/comparator.js is returning true when versions are equal. Fix gt to return true strictly when compareVersions(v1, v2) > 0."
  .task_id="HARD_T26" type="edge_case" title="[#26] Handle leading whitespace and optional leading 'v' with build metadata" file="src/parser.js" symbol="parseVersion" bug_find="const regex = /^v?(\\d+)\\.(\\d+)\\.(\\d+)(?:-([0-9A-Za-z.-]+))?(?:\\+([0-9A-Za-z.-]+))?$/;" bug_replace="const regex = /^(\\d+)\\.(\\d+)\\.(\\d+)$/;" instruction="The regex in parseVersion fails on versions with leading 'v', prerelease identifiers, and build metadata (e.g. 'v1.2.3-alpha.1+build.123'). Fix parseVersion regex to support optional 'v', prerelease tag, and build tag."
  .task_id="HARD_T27" type="unit_bug" title="[#27] Numeric vs alphanumeric prerelease identifier precedence" file="src/comparator.js" symbol="compareIdentifiers" bug_find="if (anum && bnum) {\n    const na = parseInt(a, 10);\n    const nb = parseInt(b, 10);\n    return na === nb ? 0 : na > nb ? 1 : -1;\n  }" bug_replace="if (anum && bnum) {\n    return a.length - b.length;\n  }" instruction="In compareIdentifiers (src/comparator.js), when both identifiers are numeric, they must be compared by integer value (parseInt). Fix compareIdentifiers to return 0 for equal, 1 if a > b, -1 if a < b."
  .task_id="HARD_T28" type="multi_module_regression" title="[#28] Tilde range minor bump boundary for major.minor.patch" file="src/ranges.js" symbol="parseRange" bug_find="return [{ op: '>=', version: clean.slice(1) }, { op: '<', version: `${base.major}.${base.minor + 1}.0` }];" bug_replace="return [{ op: '>=', version: clean.slice(1) }, { op: '<=', version: `${base.major}.${base.minor + 1}.0` }];" instruction="In parseRange (src/ranges.js), tilde range '~1.2.3' must strictly bound to '< 1.3.0', but currently uses '<=' allowing 1.3.0 to leak. Fix upper bound operator to strict '<'."
  .task_id="HARD_T29" type="multi_module_regression" title="[#29] maxSatisfying returning empty string on non-satisfaction" file="src/satisfier.js" symbol="maxSatisfying" bug_find="return max;" bug_replace="return max || '';" instruction="maxSatisfying in src/satisfier.js must return null (not empty string or undefined) when no version satisfies the range. Ensure it returns null if no valid version was found."
  .task_id="HARD_T30" type="edge_case" title="[#30] Strict gt() must return false for equal version inputs" file="src/comparator.js" symbol="gt" bug_find="return c !== null && c > 0;" bug_replace="return c !== null && c >= 0;" instruction="gt(v1, v2) in src/comparator.js is returning true when versions are equal. Fix gt to return true strictly when compareVersions(v1, v2) > 0."
  .task_id="HARD_T31" type="edge_case" title="[#31] Handle leading whitespace and optional leading 'v' with build metadata" file="src/parser.js" symbol="parseVersion" bug_find="const regex = /^v?(\\d+)\\.(\\d+)\\.(\\d+)(?:-([0-9A-Za-z.-]+))?(?:\\+([0-9A-Za-z.-]+))?$/;" bug_replace="const regex = /^(\\d+)\\.(\\d+)\\.(\\d+)$/;" instruction="The regex in parseVersion fails on versions with leading 'v', prerelease identifiers, and build metadata (e.g. 'v1.2.3-alpha.1+build.123'). Fix parseVersion regex to support optional 'v', prerelease tag, and build tag."
  .task_id="HARD_T32" type="unit_bug" title="[#32] Numeric vs alphanumeric prerelease identifier precedence" file="src/comparator.js" symbol="compareIdentifiers" bug_find="if (anum && bnum) {\n    const na = parseInt(a, 10);\n    const nb = parseInt(b, 10);\n    return na === nb ? 0 : na > nb ? 1 : -1;\n  }" bug_replace="if (anum && bnum) {\n    return a.length - b.length;\n  }" instruction="In compareIdentifiers (src/comparator.js), when both identifiers are numeric, they must be compared by integer value (parseInt). Fix compareIdentifiers to return 0 for equal, 1 if a > b, -1 if a < b."
  .task_id="HARD_T33" type="multi_module_regression" title="[#33] Tilde range minor bump boundary for major.minor.patch" file="src/ranges.js" symbol="parseRange" bug_find="return [{ op: '>=', version: clean.slice(1) }, { op: '<', version: `${base.major}.${base.minor + 1}.0` }];" bug_replace="return [{ op: '>=', version: clean.slice(1) }, { op: '<=', version: `${base.major}.${base.minor + 1}.0` }];" instruction="In parseRange (src/ranges.js), tilde range '~1.2.3' must strictly bound to '< 1.3.0', but currently uses '<=' allowing 1.3.0 to leak. Fix upper bound operator to strict '<'."
  .task_id="HARD_T34" type="multi_module_regression" title="[#34] maxSatisfying returning empty string on non-satisfaction" file="src/satisfier.js" symbol="maxSatisfying" bug_find="return max;" bug_replace="return max || '';" instruction="maxSatisfying in src/satisfier.js must return null (not empty string or undefined) when no version satisfies the range. Ensure it returns null if no valid version was found."
  .task_id="HARD_T35" type="edge_case" title="[#35] Strict gt() must return false for equal version inputs" file="src/comparator.js" symbol="gt" bug_find="return c !== null && c > 0;" bug_replace="return c !== null && c >= 0;" instruction="gt(v1, v2) in src/comparator.js is returning true when versions are equal. Fix gt to return true strictly when compareVersions(v1, v2) > 0."
  .task_id="HARD_T36" type="edge_case" title="[#36] Handle leading whitespace and optional leading 'v' with build metadata" file="src/parser.js" symbol="parseVersion" bug_find="const regex = /^v?(\\d+)\\.(\\d+)\\.(\\d+)(?:-([0-9A-Za-z.-]+))?(?:\\+([0-9A-Za-z.-]+))?$/;" bug_replace="const regex = /^(\\d+)\\.(\\d+)\\.(\\d+)$/;" instruction="The regex in parseVersion fails on versions with leading 'v', prerelease identifiers, and build metadata (e.g. 'v1.2.3-alpha.1+build.123'). Fix parseVersion regex to support optional 'v', prerelease tag, and build tag."
  .task_id="HARD_T37" type="unit_bug" title="[#37] Numeric vs alphanumeric prerelease identifier precedence" file="src/comparator.js" symbol="compareIdentifiers" bug_find="if (anum && bnum) {\n    const na = parseInt(a, 10);\n    const nb = parseInt(b, 10);\n    return na === nb ? 0 : na > nb ? 1 : -1;\n  }" bug_replace="if (anum && bnum) {\n    return a.length - b.length;\n  }" instruction="In compareIdentifiers (src/comparator.js), when both identifiers are numeric, they must be compared by integer value (parseInt). Fix compareIdentifiers to return 0 for equal, 1 if a > b, -1 if a < b."
  .task_id="HARD_T38" type="multi_module_regression" title="[#38] Tilde range minor bump boundary for major.minor.patch" file="src/ranges.js" symbol="parseRange" bug_find="return [{ op: '>=', version: clean.slice(1) }, { op: '<', version: `${base.major}.${base.minor + 1}.0` }];" bug_replace="return [{ op: '>=', version: clean.slice(1) }, { op: '<=', version: `${base.major}.${base.minor + 1}.0` }];" instruction="In parseRange (src/ranges.js), tilde range '~1.2.3' must strictly bound to '< 1.3.0', but currently uses '<=' allowing 1.3.0 to leak. Fix upper bound operator to strict '<'."
  .task_id="HARD_T39" type="multi_module_regression" title="[#39] maxSatisfying returning empty string on non-satisfaction" file="src/satisfier.js" symbol="maxSatisfying" bug_find="return max;" bug_replace="return max || '';" instruction="maxSatisfying in src/satisfier.js must return null (not empty string or undefined) when no version satisfies the range. Ensure it returns null if no valid version was found."
  .task_id="HARD_T40" type="edge_case" title="[#40] Strict gt() must return false for equal version inputs" file="src/comparator.js" symbol="gt" bug_find="return c !== null && c > 0;" bug_replace="return c !== null && c >= 0;" instruction="gt(v1, v2) in src/comparator.js is returning true when versions are equal. Fix gt to return true strictly when compareVersions(v1, v2) > 0."
  .task_id="HARD_T41" type="edge_case" title="[#41] Handle leading whitespace and optional leading 'v' with build metadata" file="src/parser.js" symbol="parseVersion" bug_find="const regex = /^v?(\\d+)\\.(\\d+)\\.(\\d+)(?:-([0-9A-Za-z.-]+))?(?:\\+([0-9A-Za-z.-]+))?$/;" bug_replace="const regex = /^(\\d+)\\.(\\d+)\\.(\\d+)$/;" instruction="The regex in parseVersion fails on versions with leading 'v', prerelease identifiers, and build metadata (e.g. 'v1.2.3-alpha.1+build.123'). Fix parseVersion regex to support optional 'v', prerelease tag, and build tag."
  .task_id="HARD_T42" type="unit_bug" title="[#42] Numeric vs alphanumeric prerelease identifier precedence" file="src/comparator.js" symbol="compareIdentifiers" bug_find="if (anum && bnum) {\n    const na = parseInt(a, 10);\n    const nb = parseInt(b, 10);\n    return na === nb ? 0 : na > nb ? 1 : -1;\n  }" bug_replace="if (anum && bnum) {\n    return a.length - b.length;\n  }" instruction="In compareIdentifiers (src/comparator.js), when both identifiers are numeric, they must be compared by integer value (parseInt). Fix compareIdentifiers to return 0 for equal, 1 if a > b, -1 if a < b."
  .task_id="HARD_T43" type="multi_module_regression" title="[#43] Tilde range minor bump boundary for major.minor.patch" file="src/ranges.js" symbol="parseRange" bug_find="return [{ op: '>=', version: clean.slice(1) }, { op: '<', version: `${base.major}.${base.minor + 1}.0` }];" bug_replace="return [{ op: '>=', version: clean.slice(1) }, { op: '<=', version: `${base.major}.${base.minor + 1}.0` }];" instruction="In parseRange (src/ranges.js), tilde range '~1.2.3' must strictly bound to '< 1.3.0', but currently uses '<=' allowing 1.3.0 to leak. Fix upper bound operator to strict '<'."
  .task_id="HARD_T44" type="multi_module_regression" title="[#44] maxSatisfying returning empty string on non-satisfaction" file="src/satisfier.js" symbol="maxSatisfying" bug_find="return max;" bug_replace="return max || '';" instruction="maxSatisfying in src/satisfier.js must return null (not empty string or undefined) when no version satisfies the range. Ensure it returns null if no valid version was found."
  .task_id="HARD_T45" type="edge_case" title="[#45] Strict gt() must return false for equal version inputs" file="src/comparator.js" symbol="gt" bug_find="return c !== null && c > 0;" bug_replace="return c !== null && c >= 0;" instruction="gt(v1, v2) in src/comparator.js is returning true when versions are equal. Fix gt to return true strictly when compareVersions(v1, v2) > 0."
  .task_id="HARD_T46" type="edge_case" title="[#46] Handle leading whitespace and optional leading 'v' with build metadata" file="src/parser.js" symbol="parseVersion" bug_find="const regex = /^v?(\\d+)\\.(\\d+)\\.(\\d+)(?:-([0-9A-Za-z.-]+))?(?:\\+([0-9A-Za-z.-]+))?$/;" bug_replace="const regex = /^(\\d+)\\.(\\d+)\\.(\\d+)$/;" instruction="The regex in parseVersion fails on versions with leading 'v', prerelease identifiers, and build metadata (e.g. 'v1.2.3-alpha.1+build.123'). Fix parseVersion regex to support optional 'v', prerelease tag, and build tag."
  .task_id="HARD_T47" type="unit_bug" title="[#47] Numeric vs alphanumeric prerelease identifier precedence" file="src/comparator.js" symbol="compareIdentifiers" bug_find="if (anum && bnum) {\n    const na = parseInt(a, 10);\n    const nb = parseInt(b, 10);\n    return na === nb ? 0 : na > nb ? 1 : -1;\n  }" bug_replace="if (anum && bnum) {\n    return a.length - b.length;\n  }" instruction="In compareIdentifiers (src/comparator.js), when both identifiers are numeric, they must be compared by integer value (parseInt). Fix compareIdentifiers to return 0 for equal, 1 if a > b, -1 if a < b."
  .task_id="HARD_T48" type="multi_module_regression" title="[#48] Tilde range minor bump boundary for major.minor.patch" file="src/ranges.js" symbol="parseRange" bug_find="return [{ op: '>=', version: clean.slice(1) }, { op: '<', version: `${base.major}.${base.minor + 1}.0` }];" bug_replace="return [{ op: '>=', version: clean.slice(1) }, { op: '<=', version: `${base.major}.${base.minor + 1}.0` }];" instruction="In parseRange (src/ranges.js), tilde range '~1.2.3' must strictly bound to '< 1.3.0', but currently uses '<=' allowing 1.3.0 to leak. Fix upper bound operator to strict '<'."
  .task_id="HARD_T49" type="multi_module_regression" title="[#49] maxSatisfying returning empty string on non-satisfaction" file="src/satisfier.js" symbol="maxSatisfying" bug_find="return max;" bug_replace="return max || '';" instruction="maxSatisfying in src/satisfier.js must return null (not empty string or undefined) when no version satisfies the range. Ensure it returns null if no valid version was found."
  .task_id="HARD_T50" type="edge_case" title="[#50] Strict gt() must return false for equal version inputs" file="src/comparator.js" symbol="gt" bug_find="return c !== null && c > 0;" bug_replace="return c !== null && c >= 0;" instruction="gt(v1, v2) in src/comparator.js is returning true when versions are equal. Fix gt to return true strictly when compareVersions(v1, v2) > 0."
