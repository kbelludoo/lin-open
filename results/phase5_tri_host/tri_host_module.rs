// generated by lin-rewrite → rust
#![allow(dead_code, unused_variables, unused_mut)]

pub fn collatz(n: i64) -> i64 {
    let mut count = 0; let mut cur = 0;
    count = 0;
    cur = n;
    while cur > 1 {
        if cur % 2 == 0 {
            cur = cur / 2;
        } else {
            cur = 3 * cur + 1;
        }
        count = count + 1;
    }
    return count;
}

pub fn power(base: i64, exp: i64) -> i64 {
    let mut res = 0; let mut i = 0;
    res = 1;
    i = 0;
    while i < exp {
        res = res * base;
        i = i + 1;
    }
    return res;
}


fn main() {
    let c27 = collatz(27);
    let p10 = power(2, 10);
    assert_eq!(c27, 111);
    assert_eq!(p10, 1024);
    println!("RUST_VERIFIED: collatz={} power={}", c27, p10);
}
