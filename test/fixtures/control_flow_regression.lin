@LIN_CONTROL_FLOW_REGRESSION{version=1 purpose=parser_vm_crosscheck}

!branch(x: int) -> int {
  if (x > 0) {
    return 7;
  }
  return 0;
}

!count_to_three() -> int {
  x = 0;
  while (x < 3) {
    x = x + 1;
  }
  return x;
}

=ex{branch, count_to_three}
