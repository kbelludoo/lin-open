/* generated by lia_compiler 1.0.0 */
var $K={"STRATEGY_ONE_FOR_ONE":"one_for_one","STRATEGY_ONE_FOR_ALL":"one_for_all","STRATEGY_REST_FOR_ONE":"rest_for_one","REFCAP_ISO":"iso","REFCAP_VAL":"val","REFCAP_REF":"ref","REFCAP_TAG":"tag"};
/* effect:Write */function spawnActor(name, handler, opts){var id,mailbox,alive,restartCount,maxRestarts,refcap,actor;id='actor_'+Date.now()+'_'+name;mailbox=[];alive=true;restartCount=0;maxRestarts=(opts&&opts.maxRestarts)||5;refcap=(opts&&opts.refcap)||'val';actor={id:id,name:name,alive:alive,refcap:refcap,restartCount:restartCount,mailbox:mailbox,send:null,process:null,restart:null,kill:null,status:null};actor.send=function(msg){if(actor.alive===false){return ({ok:false,reason:'dead'});};if(refcap==='iso'){mailbox.push(JSON.parse(JSON.stringify(msg)));}:if(refcap==='val'){mailbox.push(Object.freeze(msg));}else{mailbox.push(msg);};return ({ok:true})};actor.restart=function(){if(restartCount>=maxRestarts){return ({ok:false,reason:'max_restarts_exceeded',actor:id});};restartCount=restartCount+1;actor.restartCount=restartCount;actor.alive=true;actor.lastError=null;return ({ok:true,restarts:restartCount,actor:id})};actor.kill=function(){actor.alive=false;mailbox.length=0};actor.status=function(){return ({id:id,name:name,alive:actor.alive,refcap:refcap,restartCount:restartCount,mailboxSize:mailbox.length,lastError:actor.lastError||null})};return actor;}
/* effect:Write */function createSupervisor(name, strategy, opts){var children,maxRestarts,totalRestarts,sup,statuses,i;children=[];maxRestarts=(opts&&opts.maxRestarts)||10;totalRestarts=0;sup={name:name,strategy:strategy,addChild:null,status:null};sup.addChild=function(actor){children.push(actor);return sup};sup.status=function(){statuses=[];for(i=0;i<children.length;i++){statuses.push(children[i].status());};return ({name:name,strategy:strategy,totalRestarts:totalRestarts,children:statuses})};return sup;}
module.exports={spawnActor,createSupervisor};