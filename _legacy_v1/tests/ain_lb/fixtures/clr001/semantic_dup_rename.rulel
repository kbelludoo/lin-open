@RULEL:SEMANTIC_DUP_RENAME:1.0.0
~DATA
.create_fn{
  .name="moveFunds" params="from,to,amount" body="?(needsApproval(amount)){^({ok:0,need:'approval'})};^({ok:1,from:from,to:to,amount:amount})"
}
.existing_fn{
  .name="transfer" params="from,to,amount" body="?(needsApproval(amount)){^({ok:0,need:'approval'})};^({ok:1,from:from,to:to,amount:amount})"
}
.intent="create_moveFunds" target="Transfer" constraints=["no_data_loss"] allowed_effects=["memory"]
